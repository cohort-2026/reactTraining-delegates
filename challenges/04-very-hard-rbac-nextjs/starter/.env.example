# Copy this file to .env.local before running the app.
# SESSION_SECRET signs the session cookie. It must be at least 32 characters.
# This value is for local development only: generate a new one for any real
# deployment (for example with `openssl rand -base64 32`) and never commit it.
SESSION_SECRET=dev-only-secret-change-me-before-deploying-0123456789
