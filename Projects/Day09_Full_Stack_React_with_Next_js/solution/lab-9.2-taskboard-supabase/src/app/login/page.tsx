export const metadata = { title: "Log in | TaskBoard" };

export default function LoginPage() {
  return (
    <section className="mx-auto grid max-w-sm gap-4">
      <h1 className="text-3xl font-bold tracking-tight">Log in</h1>
      <p className="text-sm text-muted-foreground">
        This page is a placeholder for today. Lab 9.3 replaces it with a real
        sign up and log in form backed by Supabase Auth.
      </p>
    </section>
  );
}
